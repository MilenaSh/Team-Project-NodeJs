extends layout
block content
    main
        .container
            each c in courses
                .col-sm-6.col-md-4
                    .thumbnail
                        // image here
                        .caption
                            h3 #{c.title}
                            p #{c.lecturer}
                            p #{c.length}
                            // if statement here for like button on logged in users only